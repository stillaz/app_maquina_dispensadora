export interface Maquina {
    id: string,
    activo?: boolean,
    actualizacion?: string,
    alerta?: boolean,
    estado?: string,
    tipo?: string,
    lugar?: string,
    ubicacion?: string,
}
